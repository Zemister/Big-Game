using UnityEngine;

public class PlayerManager : MonoBehaviour //Manages the players, stats, ui, inventories, etc, (need to figure out how level will effect stats, maybe store base values of stats at each level and grab the base value depending on current player level)
{

}
