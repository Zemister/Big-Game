using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class ItemDatabase : MonoBehaviour
{
    /*
    private List<Items> database = new List<Items>();
    private GameData itemData;
    private string itemDataFileName = "itemData.json";

    void Start()
    {
        LoadGameData();
    }

    private void LoadGameData()
    {
        string filePath = Path.Combine(Applicatoin.streamingAssetsPath, itemDataFileName);

        if (File.Exists(filePath))
        {
            string dataAsJson = File.ReadAllText(filePath);
            itemData = JsonUtility.FromJson<GameData>(dataAsJson);
        } else
        {
            Debug.LogError("Cannot load game data!");
        }
    }
}

public class Items
{
*/
}
